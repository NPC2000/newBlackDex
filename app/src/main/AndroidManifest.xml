<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">

    <!-- Android 11 需要 -->
    <uses-permission
            android:name="android.permission.QUERY_ALL_PACKAGES"
            tools:ignore="QueryAllPackagesPermission" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission
            android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
            tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="top.niunaijun.blackdex.DUMP_PERMISSION" />

    <queries>
        <package android:name="top.niunaijun.blackdex32helper" />
        <provider android:authorities="top.niunaijun.blackdex32helper.dump" />
    </queries>

    <permission
            android:name="top.niunaijun.blackdex.DUMP_PERMISSION"
            android:protectionLevel="signature" />

    <application
            android:name="top.niunaijun.blackdex.app.App"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:requestLegacyExternalStorage="true"
            android:supportsRtl="true"
            android:theme="@style/Theme.BlackDex">
        <activity
                android:name="top.niunaijun.blackdex.view.base.WelcomeActivity"
                android:theme="@style/WelcomeTheme"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="top.niunaijun.blackdex.view.main.MainActivity"/>
        <activity android:name="top.niunaijun.blackdex.view.setting.SettingActivity"/>

        <provider
                android:name="androidx.core.content.FileProvider"
                android:authorities="${applicationId}.fileprovider"
                android:exported="false"
                android:grantUriPermissions="true">
            <meta-data
                    android:name="android.support.FILE_PROVIDER_PATHS"
                    android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>